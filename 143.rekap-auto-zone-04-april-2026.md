# Rekap Branch `auto-zone` → `development` — 4 April 2026

**Branch**: `auto-zone`
**Total**: 109 files changed, 10.442 insertions, 1.248 deletions
**Commits hari ini**: 4 commits (exclude merge)

---

## Ringkasan Perubahan

### 1. Multi-Courier per Delivery Zone
- Delivery Zone sekarang mendukung **banyak kurir** (sebelumnya 1 zona = 1 kurir)
- Halaman Create/Edit Delivery Zone menggunakan **checkbox** untuk memilih kurir (bukan single select)
- Tabel Delivery Zone menampilkan daftar kurir yang ditugaskan
- Pivot table baru: `delivery_zone_courier`
- Kolom `courier_id` di `delivery_zones` dibuat nullable

### 2. Galon Loan Charge — Customer Detail View
- Halaman baru: `/galon-loan-charges/customer/{id}` — menampilkan detail hutang per customer
- Fitur: summary cards, tabel charges unpaid/paid, riwayat pinjaman
- Aksi: **Mark as Paid** dan **Waive Charge** (dengan alasan)
- Route baru: `galon-loan-charges.show` dan `galon-loan-charges.customer-detail`

### 3. Return Galon Pinjaman saat Delivery & Canvassing
- Field baru `qty_loan_returned` dan `loan_return_notes` di `delivery_order_line`
- Field baru `total_galon_loan_returned` di `courier_canvassing_session`
- Kurir bisa mencatat pengembalian galon pinjaman lama saat delivery/canvassing

### 4. Hide Product dari Manufacturing
- Field baru `hidden_in_manufacturing` di `product_product`
- Setting di halaman Edit Produk untuk menyembunyikan produk dari `/manufacturing/production/create`

### 5. Employee Edit Form
- Halaman baru: `/employees/{id}/edit` — form edit karyawan
- `UpdateEmployeeRequest` dengan validasi lengkap
- Route baru: `employees.edit` dan `employees.update`

### 6. Product Seeder — Tag Tutup
- Replace istilah 'segel' dengan 'tag' di semua seeder produk
- Seeder baru: `TagTutupSeeder`
- Update BOM seeder terkait

### 7. Public API — Delivery Zones
- Route baru: `GET /api/public/delivery-zones` (throttle: 30/menit)
- Untuk auto-detect zona di landing page order

---

## Database Migrations

| No | Migration | Deskripsi |
|----|-----------|-----------|
| 1 | `add_hidden_in_manufacturing_to_product_product_table` | Field `hidden_in_manufacturing` di produk |
| 2 | `add_qty_loan_returned_to_delivery_and_canvassing_tables` | Field return galon pinjaman |
| 3 | `create_delivery_zone_courier_table` | Pivot table multi-courier per zona |
| 4 | `make_courier_id_nullable_on_delivery_zones` | Nullable courier_id di delivery_zones |

---

## Deployment Steps

```bash
git pull origin auto-zone
composer install
npm install && npm run build
php artisan migrate --force
php artisan db:seed --class=Database\\Seeders\\ProductV2\\TagTutupSeeder
php artisan optimize:clear
php artisan permission:cache-reset
```
